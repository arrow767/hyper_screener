# Hyperliquid Large Order Screener

## Overview

Скринер крупных лимитных заявок для Hyperliquid фьючерсного рынка. Мониторит orderbook всех доступных монет в реальном времени через WebSocket и отправляет алерты в Telegram при обнаружении крупных заявок.

**Основные возможности:**
- Мониторинг orderbook всех фьючерсов на Hyperliquid через WebSocket
- Фильтрация заявок по размеру (USD) и расстоянию от текущей цены (%)
- Отправка алертов в Telegram бот/канал
- Автоматическое переподключение при обрыве соединения
- Защита от спама алертов (cooldown)
- Минимальная нагрузка на API благодаря WebSocket

**Scope:**
- ✅ Мониторинг фьючерсов Hyperliquid
- ✅ Алерты в Telegram
- ✅ Настройка через .env
- ❌ Не поддерживается spot рынок

## Setup

### Requirements

- **Node.js**: >= 16.x (рекомендуется 18.x+)
- **npm** или **yarn**
- **Telegram Bot Token** (получить у [@BotFather](https://t.me/botfather))
- **Telegram Chat ID** (канал или чат для алертов)

### Install

```bash
npm install
```

### Configuration

1. Скопируйте `env.example` в `.env`:

```bash
cp env.example .env
```

2. Заполните `.env` файл:

```env
TELEGRAM_BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz
TELEGRAM_CHAT_ID=-1001234567890

MIN_ORDER_SIZE_USD=2000000
MIN_ORDER_SIZE_USD_OVERRIDES=BTC:5000000,ETH:3000000,SOL:1000000
MAX_DISTANCE_PERCENT=0.2

ALERT_COOLDOWN_MS=60000
LOG_LEVEL=info
```

**Параметры:**

| Параметр | Описание | Пример |
|----------|----------|--------|
| `TELEGRAM_BOT_TOKEN` | Токен Telegram бота от @BotFather | `123456789:ABC...` |
| `TELEGRAM_CHAT_ID` | ID чата/канала для алертов (начинается с `-` для каналов) | `-1001234567890` |
| `MIN_ORDER_SIZE_USD` | Минимальный размер заявки в USD для алерта | `2000000` (2М$) |
| `MIN_ORDER_SIZE_USD_OVERRIDES` | Кастомные минимальные размеры по монетам (`COIN:VALUE` через запятую) | `BTC:5000000,ETH:3000000` |
| `MAX_DISTANCE_PERCENT` | Максимальное расстояние от текущей цены в % | `0.2` (0.2%) |
| `ALERT_COOLDOWN_MS` | Задержка между повторными алертами для одной заявки в мс | `60000` (1 мин) |
| `LOG_LEVEL` | Уровень логирования (`info` или `debug`) | `info` |

### Getting Telegram Chat ID

**Для канала:**
1. Добавьте бота в канал как администратора
2. Отправьте сообщение в канал
3. Откройте `https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getUpdates`
4. Найдите `"chat":{"id":-1001234567890,...}`

**Для группы/личных сообщений:**
1. Напишите боту `/start` или добавьте в группу
2. Откройте `https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getUpdates`
3. Найдите chat ID в ответе

### Build

```bash
npm run build
```

### Run

**Windows (CMD/PowerShell):**

```bash
npm run dev
```

или после сборки:

```bash
npm start
```

**WSL/Linux:**

```bash
npm run dev
```

или:

```bash
node dist/index.js
```

## Usage

### Запуск мониторинга

```bash
npm run dev
```

**Вывод при запуске:**

```
============================================================
  Hyperliquid Large Order Screener
============================================================

[Main] Configuration validated successfully
[Telegram] Bot initialized: @your_bot
[Hyperliquid] Loaded 150 assets
[Hyperliquid] WebSocket connected
[Monitor] Starting orderbook monitor...
[Monitor] Min order size: $2,000,000
[Monitor] Max distance: 0.2%
[Hyperliquid] Subscribed to BTC orderbook
[Hyperliquid] Subscribed to ETH orderbook
...
[Monitor] Monitor started successfully
```

### Пример алерта в Telegram

```
🟢 LARGE BUY ORDER DETECTED

Coin: BTC
Side: BUY
Price: $36,245.5000
Size: 55.30 contracts
Value: $2.00M
Distance: 0.150%

Time: 2025-11-13T10:30:45.123Z
```

### Остановка

Нажмите `Ctrl+C` для graceful shutdown.

## Configuration Details

### MIN_ORDER_SIZE_USD

Минимальный размер заявки в USD для отправки алерта. Рассчитывается как `price * size`.

**Примеры:**
- `2000000` - алерты только для заявок >= 2М$
- `1000000` - алерты для заявок >= 1М$
- `500000` - алерты для заявок >= 500К$

### MIN_ORDER_SIZE_USD_OVERRIDES

Переопределение минимального размера заявки **по конкретным монетам**. Формат: одна строка, пары `COIN:VALUE` через запятую.

**Пример:**

```env
MIN_ORDER_SIZE_USD=2000000
MIN_ORDER_SIZE_USD_OVERRIDES=BTC:5000000,ETH:3000000,SOL:1000000
```

В этом случае:
- BTC — алерты только для заявок >= 5М$
- ETH — алерты только для заявок >= 3М$
- SOL — алерты только для заявок >= 1М$
- Все остальные монеты — используют глобальный `MIN_ORDER_SIZE_USD=2М$`

### MAX_DISTANCE_PERCENT

Максимальное расстояние от текущей (mark) цены в процентах. Заявки дальше этого расстояния игнорируются.

**Формула:**
- Для BID: `((markPrice - bidPrice) / markPrice) * 100`
- Для ASK: `((askPrice - markPrice) / markPrice) * 100`

**Примеры:**
- `0.2` - только заявки в пределах 0.2% от текущей цены (~глубина стакана Hyperliquid)
- `0.5` - заявки до 0.5% от цены
- `1.0` - заявки до 1% от цены

### ALERT_COOLDOWN_MS

Минимальный интервал между повторными алертами для одной и той же заявки (определяется по `coin-side-price`).

**Примеры:**
- `60000` - не более 1 алерта в минуту для одной заявки
- `300000` - не более 1 алерта в 5 минут
- `0` - все алерты без ограничений (не рекомендуется)

## Troubleshooting

### Ошибка: "Missing required environment variable: TELEGRAM_BOT_TOKEN"

**Причина:** Не создан `.env` файл или отсутствует переменная.

**Решение:**
1. Скопируйте `env.example` в `.env`
2. Заполните все обязательные переменные

### Ошибка: "Failed to initialize bot: 401 Unauthorized"

**Причина:** Неверный токен бота.

**Решение:**
1. Проверьте токен в `.env`
2. Создайте нового бота у [@BotFather](https://t.me/botfather) если токен утерян

### Ошибка: "Failed to send alert: 400 Bad Request: chat not found"

**Причина:** Неверный Chat ID или бот не добавлен в канал/чат.

**Решение:**
1. Для канала: добавьте бота как администратора
2. Для группы: добавьте бота в группу
3. Проверьте формат Chat ID (должен начинаться с `-` для каналов/групп)

### Нет алертов

**Причины:**
1. `MIN_ORDER_SIZE_USD` слишком высокий
2. `MAX_DISTANCE_PERCENT` слишком маленький
3. Нет крупных заявок на рынке

**Решение:**
1. Уменьшите `MIN_ORDER_SIZE_USD` для теста (например, до 100000)
2. Увеличьте `MAX_DISTANCE_PERCENT` (например, до 1.0)
3. Включите `LOG_LEVEL=debug` для отладки

### WebSocket постоянно переподключается

**Причина:** Проблемы с сетью или Hyperliquid API.

**Решение:**
1. Проверьте интернет-соединение
2. Проверьте доступность `wss://api.hyperliquid.xyz/ws`
3. Подождите, бот переподключится автоматически (до 10 попыток)

## Architecture

```
src/
├── index.ts              # Точка входа, graceful shutdown
├── config.ts             # Загрузка и валидация .env
├── types.ts              # TypeScript типы
├── hyperliquid.ts        # WebSocket клиент для Hyperliquid API
├── telegram.ts           # Telegram бот для алертов
└── orderbook-monitor.ts  # Логика мониторинга и фильтрации
```

**Архитектурные решения:**
- **WebSocket** вместо polling для минимизации нагрузки на API
- **Автореконнект** с exponential backoff при обрыве соединения
- **Cooldown** для предотвращения спама одинаковыми алертами
- **Graceful shutdown** для корректной остановки всех соединений
- **Mark price** обновляется каждые 5 секунд для точных расчетов расстояния

## Changelog

- **2025-11-13**: Initial release
  - WebSocket мониторинг orderbook для всех монет Hyperliquid
  - Фильтрация по размеру заявки (USD) и расстоянию от цены (%)
  - Telegram алерты с форматированием
  - Конфигурация через .env
  - Автореконнект и graceful shutdown

## License

MIT

