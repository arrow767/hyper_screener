# Hyperliquid Large Order Screener

## Overview

Скринер крупных лимитных заявок для Hyperliquid фьючерсного рынка. Мониторит orderbook всех доступных монет в реальном времени через WebSocket и отправляет алерты в Telegram при обнаружении крупных заявок.

**Основные возможности:**
- Мониторинг orderbook всех фьючерсов на Hyperliquid через WebSocket
- Фильтрация заявок по размеру (USD) и расстоянию от текущей цены (%)
- Отправка алертов в Telegram бот/канал
- Автоматическое переподключение при обрыве соединения
- Защита от спама алертов (cooldown)
- Минимальная нагрузка на API благодаря WebSocket

**Scope:**
- ✅ Мониторинг фьючерсов Hyperliquid
- ✅ Алерты в Telegram
- ✅ Настройка через .env
- ❌ Не поддерживается spot рынок

## Setup

### Requirements

- **Node.js**: >= 16.x (рекомендуется 18.x+)
- **npm** или **yarn**
- **Telegram Bot Token** (получить у [@BotFather](https://t.me/botfather))
- **Telegram Chat ID** (канал или чат для алертов)

### Install

```bash
npm install
```

### Configuration

1. Скопируйте `env.example` в `.env`:

```bash
cp env.example .env
```

2. Заполните `.env` файл:

```env
TELEGRAM_BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz
TELEGRAM_CHAT_ID=-1001234567890

MIN_ORDER_SIZE_USD=2000000
MIN_ORDER_SIZE_USD_OVERRIDES=BTC:5000000,ETH:3000000,SOL:1000000
MAX_DISTANCE_PERCENT=0.2

ALERT_COOLDOWN_MS=60000
LOG_LEVEL=info

# Trading module (пока только конфиг, без реальной торговли)
TRADE_ENABLED=false
TRADE_MODE=SCREEN_ONLY

TRADE_POSITION_SIZE_USD=0
TRADE_MAX_RISK_PER_TRADE=0
TRADE_MAX_OPEN_POSITIONS=1

TRADE_NATR_PERIOD=14
TRADE_NATR_TIMEFRAME=5m
TRADE_ENTRY_NATR_RANGE=[1,2]

TRADE_TP_NATR_LEVELS=2,3
TRADE_TP_PERCENTS=50,50

TRADE_SL_TICK_OFFSET=1
TRADE_DAILY_MAX_LOSS=0
TRADE_DAILY_MAX_TRADES=0
TRADE_EXECUTION_VENUE=PAPER

BINANCE_API_KEY=your_binance_api_key_here
BINANCE_API_SECRET=your_binance_api_secret_here
BINANCE_FUTURES_BASE_URL=https://fapi.binance.com
```

**Параметры:**

| Параметр | Описание | Пример |
|----------|----------|--------|
| `TELEGRAM_BOT_TOKEN` | Токен Telegram бота от @BotFather | `123456789:ABC...` |
| `TELEGRAM_CHAT_ID` | ID чата/канала для алертов (начинается с `-` для каналов) | `-1001234567890` |
| `MIN_ORDER_SIZE_USD` | Минимальный размер заявки в USD для алерта | `2000000` (2М$) |
| `MIN_ORDER_SIZE_USD_OVERRIDES` | Кастомные минимальные размеры по монетам (`COIN:VALUE` через запятую) | `BTC:5000000,ETH:3000000` |
| `MAX_DISTANCE_PERCENT` | Максимальное расстояние от текущей цены в % | `0.2` (0.2%) |
| `ALERT_COOLDOWN_MS` | Задержка между повторными алертами для одной заявки в мс | `60000` (1 мин) |
| `LOG_LEVEL` | Уровень логирования (`info` или `debug`) | `info` |
| `TRADE_ENABLED` | Включить торговый модуль (по умолчанию `false`, в текущей версии только подготовка) | `false` |
| `TRADE_MODE` | Режим работы: `SCREEN_ONLY` / `TRADE_PAPER` / `TRADE_LIVE` | `SCREEN_ONLY` |
| `TRADE_POSITION_SIZE_USD` | Базовый размер позиции в USD | `1000` |
| `TRADE_MAX_RISK_PER_TRADE` | Максимальный риск на сделку (USD или % депо, TBD в следующих спринтах) | `50` |
| `TRADE_MAX_OPEN_POSITIONS` | Максимальное число одновременных позиций | `3` |
| `TRADE_NATR_PERIOD` | Период NATR для 5m свечей | `14` |
| `TRADE_NATR_TIMEFRAME` | Таймфрейм для NATR | `5m` |
| `TRADE_ENTRY_NATR_RANGE` | Диапазон входа в NATR, формат `[min,max]` | `[1,2]` |
| `TRADE_TP_NATR_LEVELS` | Уровни тейк-профита в NATR (через запятую) | `2,3` |
| `TRADE_TP_PERCENTS` | Проценты позиции по уровням TP (через запятую) | `50,50` |
| `TRADE_SL_TICK_OFFSET` | Сдвиг SL в тиках за крупной заявкой | `1` |
| `TRADE_DAILY_MAX_LOSS` | Максимальный дневной убыток (USD), после которого торговля отключается | `0` (отключено) |
| `TRADE_DAILY_MAX_TRADES` | Максимальное количество сделок в день | `0` (отключено) |
| `TRADE_EXECUTION_VENUE` | Где исполнять ордера: `PAPER`, `HYPERLIQUID`, `BINANCE` (для `BINANCE` реализован вызов FAPI, для `HYPERLIQUID` пока заглушка) | `PAPER` |

### Getting Telegram Chat ID

**Для канала:**
1. Добавьте бота в канал как администратора
2. Отправьте сообщение в канал
3. Откройте `https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getUpdates`
4. Найдите `"chat":{"id":-1001234567890,...}`

**Для группы/личных сообщений:**
1. Напишите боту `/start` или добавьте в группу
2. Откройте `https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getUpdates`
3. Найдите chat ID в ответе

### Build

```bash
npm run build
```

### Run

**Windows (CMD/PowerShell):**

```bash
npm run dev
```

или после сборки:

```bash
npm start
```

**WSL/Linux:**

```bash
npm run dev
```

или:

```bash
node dist/index.js
```

## Usage

### Запуск мониторинга

```bash
npm run dev
```

**Вывод при запуске:**

```
============================================================
  Hyperliquid Large Order Screener
============================================================

[Main] Configuration validated successfully
[Telegram] Bot initialized: @your_bot
[Hyperliquid] Loaded 150 assets
[Hyperliquid] WebSocket connected
[Monitor] Starting orderbook monitor...
[Monitor] Min order size: $2,000,000
[Monitor] Max distance: 0.2%
[Hyperliquid] Subscribed to BTC orderbook
[Hyperliquid] Subscribed to ETH orderbook
...
[Monitor] Monitor started successfully
```

### Пример алерта в Telegram

```
🟢 LARGE BUY ORDER DETECTED

Coin: BTC
Side: BUY
Price: $36,245.5000
Size: 55.30 contracts
Value: $2.00M
Distance: 0.150%

Time: 2025-11-13T10:30:45.123Z
```

### Остановка

Нажмите `Ctrl+C` для graceful shutdown.

## Configuration Details

### MIN_ORDER_SIZE_USD

Минимальный размер заявки в USD для отправки алерта. Рассчитывается как `price * size`.

**Примеры:**
- `2000000` - алерты только для заявок >= 2М$
- `1000000` - алерты для заявок >= 1М$
- `500000` - алерты для заявок >= 500К$

### MIN_ORDER_SIZE_USD_OVERRIDES

Переопределение минимального размера заявки **по конкретным монетам**. Формат: одна строка, пары `COIN:VALUE` через запятую.

**Пример:**

```env
MIN_ORDER_SIZE_USD=2000000
MIN_ORDER_SIZE_USD_OVERRIDES=BTC:5000000,ETH:3000000,SOL:1000000
```

В этом случае:
- BTC — алерты только для заявок >= 5М$
- ETH — алерты только для заявок >= 3М$
- SOL — алерты только для заявок >= 1М$
- Все остальные монеты — используют глобальный `MIN_ORDER_SIZE_USD=2М$`

### MAX_DISTANCE_PERCENT

Максимальное расстояние от текущей (mark) цены в процентах. Заявки дальше этого расстояния игнорируются.

**Формула:**
- Для BID: `((markPrice - bidPrice) / markPrice) * 100`
- Для ASK: `((askPrice - markPrice) / markPrice) * 100`

**Примеры:**
- `0.2` - только заявки в пределах 0.2% от текущей цены (~глубина стакана Hyperliquid)
- `0.5` - заявки до 0.5% от цены
- `1.0` - заявки до 1% от цены

### ALERT_COOLDOWN_MS

Минимальный интервал между повторными алертами для одной и той же заявки (определяется по `coin-side-price`).

**Примеры:**
- `60000` - не более 1 алерта в минуту для одной заявки
- `300000` - не более 1 алерта в 5 минут
- `0` - все алерты без ограничений (не рекомендуется)

## Trading Module (Торговый модуль)

> **Новое:** торговый модуль полностью реализован с поддержкой различных режимов входа и управления лимитными ордерами!

### Режимы работы

- `TRADE_ENABLED=false` — глобальный флаг для включения торгового модуля.
- `TRADE_MODE`:
  - `SCREEN_ONLY` — только алерты, без торговли (скринер).
  - `TRADE_PAPER` — paper/dry-run режим (эмуляция сделок без реальных ордеров).
  - `TRADE_LIVE` — боевой режим с реальными ордерами (осторожно!).
- `TRADE_EXECUTION_VENUE`:
  - `PAPER` — эмуляция (работает с любым TRADE_MODE).
  - `BINANCE` — исполнение через Binance Futures API (только TRADE_LIVE).
  - `HYPERLIQUID` — исполнение через Hyperliquid (временно отключено).

### Режимы входа в позицию (TRADE_ENTRY_MODE)

**Новая функциональность!** Гибкое управление входом в позицию:

#### 1. MARKET — Только рыночные ордера

```env
TRADE_ENTRY_MODE=MARKET
TRADE_POSITION_SIZE_USD=1000
```

- Быстрый вход при обнаружении плотности
- Гарантированное исполнение
- Возможен проскальзывание

#### 2. LIMIT — Только лимитные ордера

```env
TRADE_ENTRY_MODE=LIMIT
TRADE_POSITION_SIZE_USD=1000
TRADE_ENTRY_LIMIT_NATR_RANGE=[-0.2,0.4]
TRADE_ENTRY_LIMIT_PROPORTIONS=50,50
TRADE_ENTRY_LIMIT_DENSITY_MIN_PERCENT=50
```

- Лучшая средняя цена входа
- Не гарантируется исполнение
- Автоматическая отмена при деградации плотности

#### 3. MIXED — Комбинированный вход (рекомендуется!)

```env
TRADE_ENTRY_MODE=MIXED
TRADE_ENTRY_MARKET_PERCENT=50
TRADE_ENTRY_LIMIT_PERCENT=50
TRADE_ENTRY_LIMIT_NATR_RANGE=[-0.2,0.4]
TRADE_ENTRY_LIMIT_PROPORTIONS=25,25,25,25
TRADE_ENTRY_LIMIT_DENSITY_MIN_PERCENT=50
```

- Гарантированный вход (рыночная часть) + улучшение средней цены (лимитная часть)
- Гибкое распределение рисков
- Автоматическое управление лимитками

### Параметры лимитных ордеров

#### TRADE_ENTRY_LIMIT_NATR_RANGE

Диапазон размещения лимитных ордеров в NATR от цены плотности.

**Формат:** `[min, max]` где:
- Отрицательные значения = **за плотностью** (для лонга - ниже, для шорта - выше)
- Положительные значения = **перед плотностью** (для лонга - выше, для шорта - ниже)

**Примеры:**
- `[-0.2, 0.4]` — стандартный диапазон (от -0.2 до +0.4 NATR от якорной цены)
- `[-0.1, 0.2]` — агрессивный (близко к плотности)
- `[-0.5, 0.5]` — консервативный (широкий диапазон)

#### TRADE_ENTRY_LIMIT_PROPORTIONS

Распределение объёма по лимитным ордерам.

**Примеры:**
- `50,50` — 2 лимитных ордера по 50% каждый
- `25,25,25,25` — 4 лимитных ордера по 25% каждый
- `10,20,30,40` — 4 ордера с нарастанием объёма

#### TRADE_ENTRY_LIMIT_DENSITY_MIN_PERCENT

Порог деградации плотности (%) для автоматической отмены лимитных ордеров.

**Примеры:**
- `70` — отменяем при падении до 70% (агрессивно)
- `50` — отменяем при падении до 50% (сбалансировано)
- `30` — отменяем при падении до 30% (консервативно)

### Take Profit Configuration

Настройка выхода из позиции по Take Profit с двумя режимами работы:

```env
# Уровни TP в NATR и проценты позиции на каждом уровне (обязательно)
TRADE_TP_NATR_LEVELS=2,3
TRADE_TP_PERCENTS=50,50

# Опционально: распределение TP лимитками (рекомендуется)
TRADE_TP_LIMIT_PROPORTIONS=50,50
```

**Два режима работы:**

1. **TP лимитками** (если указан `TRADE_TP_LIMIT_PROPORTIONS`):
   - TP выставляется лимитными ордерами на каждом уровне NATR
   - Распределение работает аналогично входным лимиткам
   - Пример: `TRADE_TP_LIMIT_PROPORTIONS=50,50` = 2 лимитки на каждом TP уровне
   - **Рекомендуется**: лучшая цена выхода, меньше проскальзывания

2. **TP маркет-ордерами** (если `TRADE_TP_LIMIT_PROPORTIONS` не указан):
   - Старая логика: при достижении цены TP уровня выставляется маркет-ордер
   - Гарантированное исполнение, но возможно проскальзывание
   - Автоматически используется, если лимитные пропорции не настроены

### Параметры объёма и риска

#### Фиксированный размер позиции
- `TRADE_POSITION_SIZE_USD` — базовый размер позиции в USD (используется, если `TRADE_MAX_RISK_PER_TRADE = 0`).

#### Динамический размер позиции (на основе риска) 🆕
- `TRADE_MAX_RISK_PER_TRADE` — максимальный риск на сделку в USD. Если > 0:
  - Размер позиции рассчитывается индивидуально для каждой монеты на основе NATR
  - **Автоматически включается защита от убытков** (периодический PnL-мониторинг)
  - При превышении убытка позиция закрывается автоматически
- `TRADE_RISK_NATR_MULTIPLIER` — сколько NATR использовать для расчёта расстояния до SL (по умолчанию 2).
- `TRADE_RISK_PNL_CHECK_INTERVAL_MS` — интервал проверки PnL в миллисекундах (по умолчанию 4000 = 4 секунды).

**Формула динамического размера:**
```
NATR = 0.5% (для конкретной монеты на 5m)
Множитель = 2
→ Ожидаемое движение = 0.5% × 2 = 1%

Риск = 5 USD
→ Размер позиции = 5 USD / 1% = 500 USD
```

**Пример конфигурации:**
```env
# Вариант 1: Фиксированный размер (всегда 1000 USD)
TRADE_POSITION_SIZE_USD=1000
TRADE_MAX_RISK_PER_TRADE=0  # Защита по PnL выключена

# Вариант 2: Динамический размер + защита от убытков (рекомендуется)
TRADE_POSITION_SIZE_USD=0  # Не используется
TRADE_MAX_RISK_PER_TRADE=5  # Риск 5 USD + автоматический SL по PnL
TRADE_RISK_NATR_MULTIPLIER=2  # SL на расстоянии 2 NATR
```

**Защита от убытков (Emergency Stop Loss):**

При `TRADE_MAX_RISK_PER_TRADE > 0` включается автоматический мониторинг PnL:
- ⏱️ Проверка с интервалом `TRADE_RISK_PNL_CHECK_INTERVAL_MS` (по умолчанию **4 секунды**)
- ⚠️ Если убыток превысил `TRADE_MAX_RISK_PER_TRADE` → **автоматическое закрытие**
- 🔄 Последовательность действий:
  1. Отмена всех активных лимитных ордеров (entry + TP)
  2. Закрытие позиции маркет-ордером
  3. Логирование причины закрытия с PnL

**Настройка интервала:**
```env
# Для агрессивной торговли (быстрая реакция)
TRADE_RISK_PNL_CHECK_INTERVAL_MS=2000  # 2 секунды

# Стандартный режим (рекомендуется)
TRADE_RISK_PNL_CHECK_INTERVAL_MS=4000  # 4 секунды

# Для свинг-торговли (экономия ресурсов)
TRADE_RISK_PNL_CHECK_INTERVAL_MS=10000  # 10 секунд
```

**Пример работы:**
```
Позиция: LONG BTC, entry = $50,000, size = 500 USD
Текущая цена: $49,900
PnL = -1% × 500 USD = -5 USD

TRADE_MAX_RISK_PER_TRADE = 5 USD
→ -5 USD ≥ -5 USD → ЭКСТРЕННОЕ ЗАКРЫТИЕ
```

#### Лимиты позиций
- `TRADE_MAX_OPEN_POSITIONS` — ограничение по числу одновременных позиций.

### Контекстное управление объёмом и риском (Спринт 9) 🆕

Система адаптивных правил для управления размером позиции и уровнями TP/SL на основе контекстных факторов.

#### Конфигурация YAML правил

Все правила управления контекстом теперь настраиваются через **YAML файл**, что обеспечивает максимальную гибкость:

- `POLICY_ENABLED` — включить систему контекстных правил (по умолчанию `false`)
- `POLICY_RULES_FILE` — путь к YAML файлу с правилами (по умолчанию `./policy.yaml`)
- `POLICY_ANCHOR_MEMORY_FILE` — файл для хранения статистики по якорям (по умолчанию `./data/anchor_memory.json`)
- `POLICY_MAX_WINS_PER_ANCHOR` — максимальное количество профитных сделок от одного якоря (по умолчанию `5`)

**Для начала работы:**
1. Скопируйте `policy.example.yaml` → `policy.yaml`
2. Настройте правила под свою стратегию
3. Установите `POLICY_ENABLED=true` в `.env`

**Структура правила в YAML:**
```yaml
- name: уникальное_имя_правила
  priority: 1  # меньше = выше приоритет
  scope: new_entry  # new_entry | open_position
  when:
    # Условия (все должны выполняться, логическое И)
    shock30mNatrGte: 6         # шок за 30 минут >= 6 NATR
    anchorWinCountGte: 5       # профитных сделок >= 5
  then:
    # Действия при выполнении условий
    allowTrade: false          # разрешить/запретить
    sizeMultiplier: 2.0        # множитель размера
    tpNatrMultiplier: 1.2      # множитель TP
    slNatrMultiplier: 1.0      # множитель SL
```

#### Доступные условия (when):
- `shock30mNatrGte` — минимальный шок за 30 минут (в NATR)
- `shock60mNatrGte` — минимальный шок за 60 минут (в NATR)
- `anchorTradeCountGte` — минимальное количество сделок по якорю
- `anchorWinCountGte` — минимальное количество профитных сделок по якорю
- `anchorLastTradeAgoMinGte` — минимум минут с последней сделки
- `timeInAnchorZoneMinGte` — минимум минут в зоне якоря
- `tpHitsCountEq` — точное количество достигнутых TP

#### Доступные действия (then):
- `allowTrade` — разрешить/запретить торговлю (true/false)
- `sizeMultiplier` — множитель размера позиции
- `tpNatrMultiplier` — множитель уровней TP в NATR
- `slNatrMultiplier` — множитель уровней SL в NATR

**Как работает:**

1. **Шок по NATR** — измеряет суммарную волатильность за период:
   ```
   Если за 30 минут суммарное изменение NATR >= 6:
   → Базовый размер позиции × 1.0
   
   Если за 60 минут суммарное изменение NATR >= 12:
   → Базовый размер позиции × 2.0
   → TP уровни × 1.2 (дальше)
   ```

2. **Память по якорю** — отслеживает статистику по каждому уровню (coin + price + side):
   ```
   Якорь: BTC @ $50,000 (bid)
   - Всего сделок: 3
   - Профитных: 2
   - Последняя сделка: 2 часа назад
   ```

3. **Ретест якоря** — если цена вернулась к тому же уровню через 3+ часа:
   ```
   → Уменьшаем размер позиции (× 0.5)
   → Подтягиваем TP ближе (× 0.7)
   ```

4. **Ограничение по винам** — если от якоря было 5+ профитных сделок:
   ```
   → Больше не торгуем от этого уровня
   ```

5. **Долгое стояние** — если позиция простояла 40+ минут без тейка:
   ```
   → Подтягиваем TP ближе (× 0.5)
   ```

**Файл памяти якорей (`data/anchor_memory.json`):**
```json
[
  {
    "anchorId": {
      "coin": "BTC",
      "anchorPrice": 50000,
      "side": "bid"
    },
    "totalTrades": 3,
    "winTrades": 2,
    "lossTrades": 1,
    "firstTradeAt": 1700000000000,
    "lastTradeAt": 1700010000000,
    "totalPnlUsd": 15.50,
    "avgPnlPercent": 1.2,
    "lastTradeSize": 500
  }
]
```

**Рекомендации:**
- Включайте политику только после тестирования базовой стратегии
- Начните с консервативных множителей (близких к 1.0)
- Мониторьте файл `anchor_memory.json` для анализа
- Высокий шок (12+ NATR за час) = сильный тренд = можно увеличить позицию
- Частые ретесты одного уровня = слабый уровень = уменьшаем риск

### Мониторинг новых листингов 🆕

Автоматическое отслеживание появления новых монет на Hyperliquid:

- `LISTING_MONITOR_ENABLED` — включить мониторинг новых листингов (по умолчанию `false`)
- `LISTING_NOTIFY_TELEGRAM` — отправлять уведомления в Telegram (по умолчанию `true`)
- `LISTING_CHECK_INTERVAL_MS` — интервал проверки в миллисекундах (по умолчанию `60000` = 1 минута)
- `LISTING_HISTORY_FILE` — путь к файлу истории листингов (по умолчанию `./data/listing_history.json`)

**Как работает:**
1. 🔍 Периодически проверяет список монет на Hyperliquid
2. 🆕 Сравнивает с сохранённой историей
3. 📱 Отправляет уведомление в Telegram при обнаружении новой монеты
4. ✅ Отправляет уведомление только **один раз** (без спама)
5. 💾 Сохраняет историю в JSON файл

**Пример уведомления в Telegram:**
```
🆕 Новый листинг на Hyperliquid!

💎 Монета: PEPE
⏰ Обнаружено: 17.11.2024, 14:30:45 UTC

🔍 Начинаем мониторинг плотностей...
```

**Множественные листинги:**
```
🆕 Новые листинги на Hyperliquid!

💎 Монеты (3):
• WIF
• BONK
• POPCAT

⏰ Обнаружено: 17.11.2024, 14:30:45 UTC
🔍 Начинаем мониторинг плотностей...
```

**Рекомендации:**
- Интервал 60 секунд (1 минута) — оптимальный для большинства случаев
- Для быстрой реакции можно уменьшить до 30 секунд
- История хранится в `./data/listing_history.json` — не удаляйте этот файл!
- При первом запуске все текущие монеты добавляются в историю без уведомлений

### Логирование сделок в CSV 🆕

Автоматическое логирование всех закрытых сделок в CSV файлы (по дням):

- `TRADE_LOG_ENABLED` — включить/выключить логирование (по умолчанию `true`)
- `TRADE_LOG_DIR` — директория для сохранения логов (по умолчанию `./trade_logs`)

**Структура CSV файла:**
```csv
tradeId,coin,side,openedAt,closedAt,durationSeconds,durationMinutes,
entryPrice,exitPrice,sizeUsd,pnlUsd,pnlPercent,entryFeeUsd,exitFeeUsd,
totalFeeUsd,natr,maxDrawdownPercent,closeReason,entryMode,signalSource,
limitOrdersUsed,tpLevelsHit
```

**Файлы по дням:**
```
./trade_logs/
  ├── trades_2024-01-15.csv
  ├── trades_2024-01-16.csv
  └── trades_2024-01-17.csv
```

**Содержит метрики:**
- 📊 Базовые: тикер, направление (long/short), цены входа/выхода
- ⏱️ Время: дата/время открытия и закрытия, длительность в секундах и минутах
- 💰 Финансы: объём в USD, PnL в USD и %, комиссии (если известны)
- 📈 Технические: NATR на момент входа, причина закрытия
- 🔧 Контекст: режим входа (market/limit/mixed), количество использованных лимитных ордеров, достигнутые TP уровни

**Причины закрытия (closeReason):**
- `tp_hit`, `tp_all_hit`, `tp_limit_all_hit` — достигнут take profit
- `emergency_stop_loss_pnl=-5.00$` — экстренное закрытие по PnL
- `anchor_removed_from_book_in_view` — плотность исчезла из стакана
- `anchor_value_below_threshold` — деградация плотности ниже порога
- `anchor_lost_out_of_view_against` — цена ушла против позиции

### Параметры NATR и уровней

- `TRADE_NATR_PERIOD` — период NATR (по умолчанию 14).
- `TRADE_NATR_TIMEFRAME` — таймфрейм для NATR (по умолчанию `5m`).
- `TRADE_TP_NATR_LEVELS` и `TRADE_TP_PERCENTS` — уровни TP в NATR и проценты позиции.

### Дневные лимиты

- `TRADE_DAILY_MAX_LOSS` — дневной лимит убытка (USD).
- `TRADE_DAILY_MAX_TRADES` — лимит числа сделок в день.

### Примеры конфигураций

Смотрите [CONFIG_EXAMPLES.md](./CONFIG_EXAMPLES.md) для детальных примеров различных торговых стратегий.

## Troubleshooting

### Ошибка: "Missing required environment variable: TELEGRAM_BOT_TOKEN"

**Причина:** Не создан `.env` файл или отсутствует переменная.

**Решение:**
1. Скопируйте `env.example` в `.env`
2. Заполните все обязательные переменные

### Ошибка: "Failed to initialize bot: 401 Unauthorized"

**Причина:** Неверный токен бота.

**Решение:**
1. Проверьте токен в `.env`
2. Создайте нового бота у [@BotFather](https://t.me/botfather) если токен утерян

### Ошибка: "Failed to send alert: 400 Bad Request: chat not found"

**Причина:** Неверный Chat ID или бот не добавлен в канал/чат.

**Решение:**
1. Для канала: добавьте бота как администратора
2. Для группы: добавьте бота в группу
3. Проверьте формат Chat ID (должен начинаться с `-` для каналов/групп)

### Нет алертов

**Причины:**
1. `MIN_ORDER_SIZE_USD` слишком высокий
2. `MAX_DISTANCE_PERCENT` слишком маленький
3. Нет крупных заявок на рынке

**Решение:**
1. Уменьшите `MIN_ORDER_SIZE_USD` для теста (например, до 100000)
2. Увеличьте `MAX_DISTANCE_PERCENT` (например, до 1.0)
3. Включите `LOG_LEVEL=debug` для отладки

### WebSocket постоянно переподключается

**Причина:** Проблемы с сетью или Hyperliquid API.

**Решение:**
1. Проверьте интернет-соединение
2. Проверьте доступность `wss://api.hyperliquid.xyz/ws`
3. Подождите, бот переподключится автоматически (до 10 попыток)

## Architecture

```
src/
├── index.ts              # Точка входа, graceful shutdown
├── config.ts             # Загрузка и валидация .env
├── types.ts              # TypeScript типы
├── hyperliquid.ts        # WebSocket клиент для Hyperliquid API
├── telegram.ts           # Telegram бот для алертов
└── orderbook-monitor.ts  # Логика мониторинга и фильтрации
```

**Архитектурные решения:**
- **WebSocket** вместо polling для минимизации нагрузки на API
- **Автореконнект** с exponential backoff при обрыве соединения
- **Cooldown** для предотвращения спама одинаковыми алертами
- **Graceful shutdown** для корректной остановки всех соединений
- **Mark price** обновляется каждые 5 секунд для точных расчетов расстояния

## Changelog

- **2025-11-13**: Initial release
  - WebSocket мониторинг orderbook для всех монет Hyperliquid
  - Фильтрация по размеру заявки (USD) и расстоянию от цены (%)
  - Telegram алерты с форматированием
  - Конфигурация через .env
  - Автореконнект и graceful shutdown

## License

MIT

